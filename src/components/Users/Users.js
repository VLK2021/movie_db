import React from 'react';
// import {useSelector} from "react-redux";

import User from "../User/User";


const Users = () => {
    // const {users} = useSelector(store => store.usersAll);

    return (
        <>
            <User/>
        </>
    );
};

export default Users;